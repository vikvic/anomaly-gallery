Holds the screenshot referenced by the root README.

`screenshot.png` — the game mid-round, ideally with a graft already found so the
anomaly is ringed. Capture the canvas and the HUD, not the whole browser window.
